# COPR repos to enable before dnf install
# Format: owner/repo
#
# Most Hyprland packages are in Fedora main repos as of Fedora 40+.
# Add COPRs only for packages that aren't (e.g. a newer hyprland build,
# or ghostty if you don't use the install script).

# scottames/ghostty     # uncomment if you want COPR ghostty instead of build-from-source
